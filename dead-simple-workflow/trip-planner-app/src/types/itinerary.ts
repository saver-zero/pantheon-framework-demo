export interface ItineraryResponse {
  markdown: string;
}

export interface ItineraryMetadata {
  destination: string;
  partyInfo: string;
  month: string;
  days: number;
  generatedAt: string;
  markdown: string;
}
