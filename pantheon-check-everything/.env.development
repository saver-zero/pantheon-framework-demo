# Development Environment Configuration
# This file is used during local development with Vite dev server

# Backend API URL
# Set to empty string to use Vite proxy (relative URLs like /api/itinerary)
# The Vite proxy forwards /api requests to http://localhost:3001
VITE_BACKEND_URL=
